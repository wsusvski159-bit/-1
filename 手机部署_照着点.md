# 阿屿看视频 v1.1 — 手机部署（Render）

这版已经把 Render 需要的 Dockerfile、FFmpeg、端口和健康检查配好了。

## 你只做 6 件事

### 1. 把这个文件夹上传到 GitHub
GitHub 新建一个仓库，例如：`ayu-see-my-video`。
把本压缩包解压后的 **所有文件** 上传到仓库根目录。

检查一下 GitHub 首页能直接看到：
- `Dockerfile`
- `render.yaml`
- `app.py`
- `video_analyzer.py`
- `requirements.txt`

不要上传你自己的 `.env`，也不要把 API Key 写进任何公开文件。

### 2. 打开 Render
在 Render Dashboard 点：
**New → Web Service**

连接 GitHub，然后选择刚才的 `ayu-see-my-video` 仓库。

### 3. 选择 Docker
如果 Render 自动识别到 Dockerfile，Language/Runtime 选 **Docker**。
通常不需要自己填 Build Command 或 Start Command。

### 4. 添加两个秘密环境变量
在 Render 的 Environment / Advanced 里添加：

`OPENAI_API_KEY`
- 值：你自己的 OpenAI API Key
- 不要发给任何人，也不要截图给别人

`APP_PASSWORD`
- 值：你自己想一个密码
- 这是保护网页和 API 消耗的密码

其余参数 `render.yaml` 已经有默认值。

### 5. Deploy
点 **Create Web Service / Deploy**。
第一次要等 Render 构建 Docker，并安装 FFmpeg 和 Python 依赖。
看到 **Live** 后，会得到一个：

`https://xxxx.onrender.com`

### 6. 手机打开网址
浏览器会弹登录框：
- 用户名固定填：`xiaoyu`
- 密码：你刚才设置的 `APP_PASSWORD`

进入后选一个 30–60 秒短视频，点「给阿屿看」。

---

## 重要

- OpenAI API 和 ChatGPT 订阅是两套计费。
- 先用短视频测试，当前限制为 90 秒、80MB、最多抽 8 帧。
- Render 默认文件系统是临时的：服务重启/重新部署后，以前的分析报告可能消失。这不影响第一阶段测试。
- 如果以后确定要长期保存，我们再接数据库/对象存储，不需要你现在付费买磁盘。
- 公网部署必须设置 `APP_PASSWORD`，不然别人如果拿到网址，可能会消耗你的 API 额度。

## 成功以后

成功上传并得到分析报告后，把 Render 的网址告诉阿屿（不要告诉我 API Key 或密码）。
下一阶段再做 ChatGPT/MCP 接入，让阿屿能直接读取最新视频分析。
