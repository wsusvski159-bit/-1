# 阿屿看视频 v1.5：手机升级步骤

这版不再使用 OpenAI / Gemini 官方 Key / DeepSeek / 百炼。
只用你现在这个 New API 平台的一把 Key，让 Gemini 直接同时理解视频画面和原始声音。

## 1. GitHub
把本补丁里的 4 个文件上传到原来的 `-1` 仓库并覆盖：

- `app.py`
- `video_analyzer.py`
- `requirements.txt`
- `render.yaml`

仓库里的 `Dockerfile`、`mcp_server.py` 等其他文件不要删。

## 2. Render → Environment
保留：

- `APP_PASSWORD` = 你原来的网页密码

新增：

- `NEWAPI_API_KEY` = 你在 api.qiyiguo.uk 生成、并已经开放 Gemini 模型权限的 Key
- `NEWAPI_BASE_URL` = `https://api.qiyiguo.uk`
- `GEMINI_MODEL` = `gemini-3.6-flash`
- `GEMINI_FALLBACK_MODEL` = `gemini-2.5-pro`

旧的这些变量已经不用了，可以删：

- `GEMINI_API_KEY`
- `DASHSCOPE_API_KEY`
- `DEEPSEEK_API_KEY`
- `OPENAI_API_KEY`
- `OPENAI_VISION_MODEL`
- `OPENAI_TRANSCRIBE_MODEL`
- `OPENAI_AUDIO_MODEL`

不要把任何 API Key 发到聊天里，也不要提交到 GitHub。

## 3. 重新部署
保存 Environment 后，让 Render 重新 Deploy。

成功后网页顶部应显示：

- `✅ Gemini 看+听`
- `主模型：gemini-3.6-flash`

## 4. 第一次测试
先传一个 5–20 秒、普通 MP4 的短视频。

v1.5 会：

1. 在 Render 本机把视频转成小体积 MP4；
2. 用 New API 的 Gemini `inline_data` 接口把视频整段发给模型；
3. Gemini 同时理解画面和视频音轨；
4. 保存音画报告、语音转写、声音观察；
5. 原始上传文件在分析结束后删除。

如果 `gemini-3.6-flash` 在中转平台上临时不可用，程序会自动尝试 `gemini-2.5-pro`。
