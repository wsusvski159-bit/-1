# v1.5.1 FFmpeg 热修

只需要覆盖 GitHub `-1` 仓库中的两个文件：

- `app.py`
- `video_analyzer.py`

环境变量全部保持不变。

修复内容：
- 小体积 MP4 不再强制二次转码，直接给 Gemini。
- 大视频转码改为 720p / ultrafast / 单线程，降低 Render Free 的 CPU/RAM 压力。
- 如果 FFmpeg 仍失败，网页会直接显示真正的 stderr 原因，不再只显示一长串命令和 exit status。
