# 手机升级：v1.1 → v1.2

你已经把 Render 部署跑通了，所以这次不用重建服务。

## 只做 4 件事

1. 在 GitHub 仓库替换 4 个文件：
   - video_analyzer.py
   - app.py
   - requirements.txt
   - render.yaml

2. Render → Environment 新增：
   - GEMINI_API_KEY = 你的 Gemini API Key
   - DEEPSEEK_API_KEY = 你的 DeepSeek API Key
   保留 APP_PASSWORD。

3. 保存 Environment。Render 会重新部署；如果没有自动部署，Manual Deploy → Deploy latest commit。

4. 变成 Live 后打开网址。首页应该显示：
   - ✅ Gemini 眼睛
   - ✅ DeepSeek 整理
   - ✅ 网页密码

然后先传 30 秒左右的视频测试。

注意：API Key 不要发给别人，也不要截图公开。
